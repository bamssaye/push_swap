// rra
    // while (index > 0)
    // {
    //     tmp = arr[index];
    //     arr[index] = arr[index - 1];
    //     arr[index - 1] = tmp;
    //     index--;
    // }
    //ra
     int i = 0;
    // while (i < index - 1)
    // {
    //     tmp = arr[i];
    //     arr[i] = arr[i + 1];
    //     arr[i + 1] = tmp;
    //     i++;
    // }
    //swap
    // tmp = arr[i];
    // arr[i] = arr[i + 1];
    // arr[i + 1] = tmp;


https://saadloukili.github.io/Push-Swap-Visualizer/random.html